import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  display: grid;
  place-items: center;
  min-height: 100vh;
  padding: 3rem;
  gap: 2rem;
`;
export default { Container };
